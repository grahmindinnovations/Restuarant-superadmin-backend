This folder is reserved for your data models (for example, Mongoose schemas or Prisma models).

Right now the backend only sends emails and validates the super admin login using environment variables, so there is no database model code yet.

